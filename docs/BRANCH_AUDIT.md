# Branch audit

The fresh source clone exposed exactly one branch: `main`. There were no
alternate implementation, experiment, baseline, or release branches to map.

The final publication keeps only `main`; its README, claim contract, source
pins, evidence, and gate all describe the same audited state. The branch is
renamed only at the repository level; no branch-specific result was discarded.
